# ドッカンバトル ダメージ計算 API - 環境変数設定例
# 実際の使用時は .env ファイルにコピーして値を設定してください

# アプリケーション設定
APP_NAME="ドッカンバトル ダメージ計算 API"
APP_VERSION="1.0.0"
DEBUG=true

# サーバー設定
HOST=0.0.0.0
PORT=8000

# CORS設定（カンマ区切りで複数指定可能）
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 外部API設定
EXTERNAL_API_BASE_URL=https://api.dokkan.example.com
EXTERNAL_API_TIMEOUT=30
EXTERNAL_API_RETRY_COUNT=3

# データベース設定（将来の拡張用）
DATABASE_URL=postgresql://user:password@localhost/dokkan_calc

# キャッシュ設定
CACHE_TTL=3600

# ログ設定
LOG_LEVEL=INFO